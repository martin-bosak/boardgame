# Terraforming Mars Board Game

This is a web-based implementation of the board game Terraforming Mars.

## Project Setup

1.  Install Python dependencies:
    ```bash
    pip install -r requirements.txt
    ```

2.  Run the application:
    ```bash
    python app.py
    ``` 